"""
Professor, I looked back through the repo, and I think your concern is mostly fair. I did not write true end-to-end HTTP integration tests where a real Spring server starts and requests go through actual endpoints over a network port. Most of my tests are unit-style tests. For the HTTP client parts, I used Spring’s MockRestServiceServer, which is a built-in tool that intercepts RestTemplate calls and lets me verify the URL, method, and payload without making a real network connection. I used that because it makes tests faster, more deterministic, and easier to debug. So I agree that I am not fully testing the web or controller layer the way the class examples do. At the same time, the code does use HTTP in production, and I did test that behavior in a mocked way rather than through a live server. So I think the missing piece is true web-layer integration testing, and the next step would be adding one or two Spring tests that exercise the real endpoint wiring

“I think you’re right that I didn’t fully test the controller layer over a running web app, but RemoteParticipant itself does use HTTP. It posts to the remote client’s /action and /reset endpoints. I tested that with Spring’s mocked HTTP tool instead of a live server, so the missing part is true web integration testing, not networking logic entirely.”
"""